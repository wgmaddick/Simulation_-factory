# Simulation_-factory

Streamlit multipage simulation apps live at the repo root (`app.py`, `pages/`).

## System Analysis API

Node/Express **Video + Talk + Send** engine with Twin-Track output and Passive Anomaly Sentinel:

```bash
cd system_analysis
npm install
npm start
```

See [`system_analysis/README.md`](system_analysis/README.md).
